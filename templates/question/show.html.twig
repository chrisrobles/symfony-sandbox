{% extends 'base.html.twig' %} {# Template Inheritance, allows most pages to share an HTML layout #}
{% block title %}Question: {{ question }}{% endblock %}
{% block body %}

    <h1>{{ question }}</h1>

    {# oh, I'm just a comment hiding here #}

    <h2>Answers {{ answers|length }}</h2>
    <ul>
        {% for index, answer in answers %}
            <li>{{ answer }}</li>
        {% endfor %}
    </ul>
{% endblock %}